

export class Store {
    constructor(
        public id: string,
        public name: string,
        public email: string,
        public address: string,
        public ownerId: string
    ){};
}